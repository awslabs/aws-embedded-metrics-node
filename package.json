{
  "name": "aws-embedded-metrics",
  "version": "0.1.0-beta.1571425060",
  "description": "AWS Embedded Metrics Package",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "repository": {},
  "scripts": {
    "test": "jest --config jestconfig.json",
    "integ": "./bin/run-integ-tests.sh",
    "exec-integ": "jest --config jestconfig.integ.json",
    "watch": "npm run test -- --watch",
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "npm run fix-lint && tslint -p tsconfig.json",
    "fix-lint": "prettier-tslint fix 'src/**/*.ts'",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format"
  },
  "keywords": [],
  "author": "jaredcnance",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/faker": "^4.1.5",
    "@types/jest": "^24.0.15",
    "@types/node": "^12.0.8",
    "aws-sdk": "^2.551.0",
    "faker": "^4.1.0",
    "jest": "^24.8.0",
    "npm-pack-zip": "^1.2.7",
    "prettier": "^1.18.2",
    "prettier-tslint": "^0.4.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.17.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.5.2"
  },
  "files": [
    "lib/**/*"
  ],
  "dependencies": {},
  "volta": {
    "node": "10.16.0"
  }
}
